services:
  predictiq:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: predictiq-enterprise
    restart: always
    ports:
      - "3000:3000"
    volumes:
      - ./frontend/data:/app/frontend/data
    environment:
      - NODE_ENV=production
      - PORT=3000
